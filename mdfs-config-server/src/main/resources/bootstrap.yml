server.port: 8888

spring:
  application:
     name: myconfigserver
  profiles:
     active: native

---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
